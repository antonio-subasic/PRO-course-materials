<Window x:Class="HelloWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelloWPF"
        mc:Ignorable="d"
        Background="Lime"
        Title="Hello WPF" Height="75" Width="250">
    <Viewbox>
        <StackPanel Orientation="Horizontal">
            <TextBlock Name="FirstNumber" VerticalAlignment="Center" Margin="10 0 0 0" />
            <TextBlock Name="Operator" VerticalAlignment="Center" />
            <TextBlock Name="SecondNumber" VerticalAlignment="Center" />
            <TextBlock Text="=" VerticalAlignment="Center" />
            <TextBox
                Name="ResultTextBox"
                Width="50"
                Margin="5"
                Background="Red"
                Foreground="White"
                VerticalContentAlignment="Center"
                BorderThickness="0"
            />
            <Button Margin="0 5 5 5" Padding="10 0" Click="OnCheck">Check</Button>
        </StackPanel>
    </Viewbox>
</Window>
